if [ -n "$ZSH_VERSION" ]; then
   shell=zsh
elif [ -n "$BASH_VERSION" ]; then
   shell=bash
fi

echo "\necho "Loading aliases"" >> ~/."$shell"rc
echo "source ~/Portal/init/.aliases" >> ~/."$shell"rc
 
